pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --new-from-rev=HEAD ./...
    tests:
      glob: "*.go"
      run: go test -short ./...
    bpflint:
      glob: "*.{c,h}"
      root: "internal/bpf/"
      run: bpflinter {staged_files} || true
