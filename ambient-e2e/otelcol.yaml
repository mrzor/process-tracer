receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:14318

exporters:
  file:
    path: ./staging/traces.jsonl

service:
  telemetry:
    metrics:
      level: none
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
